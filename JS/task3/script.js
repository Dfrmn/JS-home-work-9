let num = prompt(`Введите целое число`);

console.log(num > 0 ? (`Число ${num} положительное`) : (`Число ${num} не положительное`));

// String(num);

console.log(`длина строки которую ввел юзер: ` + num.length);